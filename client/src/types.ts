export type FeatureToggles = { customers: boolean; transactions: boolean; bills: boolean; inventory: boolean; reports: boolean; notifications: boolean };
export type Language = 'en' | 'hi' | 'gu' | 'hinglish';
export type User = { id: string; name: string; email: string; mobile: string; photo?: string; theme: string; mode: string; customColor?: string; fontFamily: string; fontScale: number; language: Language; accountMode: 'personal' | 'business'; showContactDetails: boolean; featureToggles: FeatureToggles; contactChanges?: { kind: 'email' | 'mobile'; at: string }[]; lastContactChangeAt?: string; googleLinked?: boolean };
export type Customer = { photo?: string; linkedUserId?: string; upiId?: string; upiQr?: string; _id: string; name: string; mobile: string; email?: string; address?: string; note?: string; balance: number; archived?: boolean; dueDate?: string; createdAt: string };
export type Entry = { sourceEntryId?: string; actorId?: string; attachmentId?: string; action?: 'udhar' | 'advance' | 'payment'; _id: string; customerId: string; kind: 'given' | 'received' | 'reversal'; amount: number; delta: number; note: string; date: string; createdAt: string; reversalOf?: string; billId?: string };
export type Product = { _id: string; name: string; sku: string; price: number; cost: number; stock: number; minimum: number; unit: string };
export type Bill = { _id: string; customerId: string; number: string; items: { name: string; quantity: number; price: number; taxRate: number }[]; subtotal: number; tax: number; total: number; note?: string; dueDate?: string; createdAt: string };
export type PaymentRequest = { _id: string; customerId: string; amount: number; currency: 'INR'; upiId: string; intent: string; status: 'created' | 'opened' | 'paid' | 'cancelled'; note?: string; createdAt: string; openedAt?: string; paidAt?: string; entryId?: string };
export type Workspace = { business: { _id: string; name: string; phone?: string; email?: string; address?: string; gstin?: string; logo?: { url: string }; upiId?: string; upiQr?: { url: string } }; customers: Customer[]; entries: Entry[]; products: Product[]; bills: Bill[]; paymentRequests: PaymentRequest[]; notifications: { _id: string; title: string; body: string; href: string; readAt?: string; createdAt: string }[]; totals: { receivable: number; advance: number; customers: number }; monthly: { _id: string; given: number; received: number }[]; recordLimit: number };
